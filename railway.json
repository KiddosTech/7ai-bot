{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install"
  },
  "deploy": {
    "startCommand": "rm -rf auth_info_baileys && node index.js",
    "restartPolicyType": "ALWAYS"
  }
}
